# train_hospital - 医院数据预处理后的训练数组

## 数据内容
从医院心电数据提取的心搏片段，已转换为 numpy 数组格式，可直接用于模型训练。

## 文件列表
| 文件名 | 大小 | 说明 |
|---|---|---|
| `X_0.npy` | 5.6 GB | 特征数组（ECG 心搏波形片段） |
| `Y_0.npy` | 1.9 MB | 标签数组（心搏分类标签） |

## 总大小
约 5.6 GB

## 如何获取
由 `code/generate_beat_data.py` 从 `real_data/` 目录的 .DATA 文件处理生成。
